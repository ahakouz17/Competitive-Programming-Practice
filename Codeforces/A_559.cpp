#include <iostream>
#include <cmath>
#include <stdio.h>

using namespace std;

int main()
{
    int a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    cout << (c + d + e) * (c + d + e) - (a * a) - (c * c) - (e * e) << endl;
    return 0;
}